# Print-confirm verdicts (Now #2)

One file per frozen list under `sim/decks/`. Written after a Limitless print-compare.
A matchup may run Now #1 only when its two lists are `verdict: "clear"` (or the
blocked side is not in that run). Cosmetic and already-parked misses do not block.
Firing misses do.

Schema is the JSON next to this file. Do not invent an Ace ladder cell from these.
